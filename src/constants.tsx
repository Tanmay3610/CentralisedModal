export enum ModalTypes {
  MODAL,
  BOTTOM_SHEET,
  DIALOG_BOX,
  SIDE_BAR
}

export enum ModalHorizontalPosition {
  LEFT,
  RIGHT,
  CENTER,
  CUSTOM
}

export enum ModalVerticalPosition {
  TOP,
  BOTTOM,
  CENTER,
  CUSTOM
}

export enum ChildrenHorizontalPosition {
  CENTER,
  LEFT,
  RIGHT
}

export enum ChildrenVerticalPosition {
  CENTER,
  TOP,
  BOTTOM
}

export enum ModalCloseButtonVerticalPosition {
  TOP,
  BOTTOM,
}

export enum ModalCloseButtonHorizontalPosition {
  LEFT,
  RIGHT,
  CENTER
}

export enum SlideAnimationDirection {
  TOP_BOTTOM,
  BOTTOM_TOP,
  LEFT_RIGHT,
  RIGHT_LEFT
}

export enum WidthType {
  FIT_CONTENT,
  COVER,
  CUSTOM
}

export enum HeightType {
  FIT_CONTENT,
  COVER,
  CUSTOM
}

export enum ModalWrapperAnimations {
  FADE_IN,
  SLIDE_IN,
  NONE
}

export enum ModalOverlayAnimation {
  FADE_IN,
  NONE
}

export const ModalSource = 'modal';
